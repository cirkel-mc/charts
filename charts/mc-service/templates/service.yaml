apiVersion: v1
kind: Service
metadata:
  name: {{ include "mc-service.fullname" . }}
  labels:
    {{- include "mc-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mc-service.selectorLabels" . | nindent 4 }}
